### Rebind leader ###
unbind C-b
set -g prefix C-a
bind C-a send-prefix

### Terminal settings ###
set -g default-terminal "xterm-256color"
set-option -ga terminal-overrides ",xterm-256color:Tc"

### Start window and pane indexing at 1 ###
set -g base-index 1
setw -g pane-base-index 1

### Mouse support ###
set -g mouse on

### Rebind % to : ###
unbind %
bind : split-window -h

# Scroll wheel fix for atuin etc.
bind -T root WheelUpPane if-shell -F -t= "#{pane_in_mode}" \
  "send-keys -M" "copy-mode -e"

# Smart pane switching with awareness of Vim splits.
# See: https://github.com/christoomey/vim-tmux-navigator
vim_pattern='(\S+/)?g?\.?(view|l?n?vim?x?|fzf)(diff)?(-wrapped)?'
is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    | grep -iqE '^[^TXZ ]+ +${vim_pattern}$'"
bind-key -n 'C-h' if-shell "$is_vim" 'send-keys C-h'  'select-pane -L'
bind-key -n 'C-j' if-shell "$is_vim" 'send-keys C-j'  'select-pane -D'
bind-key -n 'C-k' if-shell "$is_vim" 'send-keys C-k'  'select-pane -U'
bind-key -n 'C-l' if-shell "$is_vim" 'send-keys C-l'  'select-pane -R'
tmux_version='$(tmux -V | sed -En "s/^tmux ([0-9]+(.[0-9]+)?).*/\1/p")'
if-shell -b '[ "$(echo "$tmux_version < 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\'  'select-pane -l'"
if-shell -b '[ "$(echo "$tmux_version >= 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\\\'  'select-pane -l'"

bind-key -T copy-mode-vi 'C-h' select-pane -L
bind-key -T copy-mode-vi 'C-j' select-pane -D
bind-key -T copy-mode-vi 'C-k' select-pane -U
bind-key -T copy-mode-vi 'C-l' select-pane -R
bind-key -T copy-mode-vi 'C-\' select-pane -l

# Resize with prefix + hjkl
bind -r h resize-pane -L 5
bind -r j resize-pane -D 5
bind -r k resize-pane -U 5
bind -r l resize-pane -R 5

### Custom keybindings ###
bind-key t run-shell 'tracktime'
bind-key r run-shell 'chezmoi apply && tmux source-file ~/.tmux.conf && tmux display-message "Chezmoi applied & config reloaded!"'

# Status bar at the top
set-option -g status-position top

# Gruvbox colors
set -g status-style "bg=#282828,fg=#ebdbb2"

# Left side: leave empty or show session name
set -g status-left-length 80
set -g status-left "#[fg=#d65d0e]#[bold] #S"

# Right side: date & time
set -g status-right-length 60
set -g status-right "#[fg=#d65d0e]#[bold]%Y-%m-%d #[fg=#d65d0e]#[bold]%H:%M #[default]"

# Center the tabs by padding left/right
set -g status-justify centre
setw -g window-status-format " #I:#W "
setw -g window-status-current-format "#[fg=#fe8019,bold] #I:#W #[default]"

# Pane border colors
set -g pane-border-style "fg=#928374"
set -g pane-active-border-style "fg=#fabd2f"

# Message styling
set -g message-style "bg=#3c3836,fg=#fbf1c7"

# Copy mode style
set -g mode-style "bg=#665c54,fg=#fabd2f"
